apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: search-service-policy
  namespace: app
spec:
  podSelector:
    matchLabels:
      app: search-service
  policyTypes:
    - Ingress
    - Egress

  ingress:
    # Allow HTTP from NGINX Ingress Controller (external traffic routed via Ingress)
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ingress-nginx
      ports:
        - port: 8080
          protocol: TCP

    # Allow HTTP from BFF (search queries proxied through the API gateway)
    - from:
        - podSelector:
            matchLabels:
              app: bff
      ports:
        - port: 8080
          protocol: TCP

    # Allow kubelet health probes (no namespace selector — kubelet runs on the node)
    - ports:
        - port: 8080
          protocol: TCP

  egress:
    # Allow reads from PostgreSQL (search queries run against the DB)
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: data
          podSelector:
            matchLabels:
              app: postgres
      ports:
        - port: 5432
          protocol: TCP

    # Allow DNS resolution
    - ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
