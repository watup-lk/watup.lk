apiVersion: v1
kind: ConfigMap
metadata:
  name: stats-service-config
  namespace: app
data:
  PORT: "8080"
  # PostgreSQL connection — host points to the ClusterIP service in the data namespace
  DATABASE_URL: "postgres://watup_user:watup_dev_password@postgres-service.data.svc.cluster.local:5432/watup_db?search_path=salary_schema&sslmode=disable"
