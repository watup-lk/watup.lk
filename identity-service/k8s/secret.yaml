# ⚠️  IMPORTANT: These are placeholder base64-encoded values for local/dev use ONLY.
# In production, JWT_SECRET and DATABASE_URL are loaded from Azure Key Vault at runtime
# via Managed Identity (see AZURE_KEYVAULT_URL in configmap.yaml).
# Never commit real credentials to version control.
#
# To encode your own values:  echo -n "your-value" | base64
apiVersion: v1
kind: Secret
metadata:
  name: identity-service-secret
  namespace: app
type: Opaque
data:
  # JWT signing key — replace with a strong random key (min 32 chars) before deploying
  # echo -n "your-strong-jwt-secret-key-here" | base64
  JWT_SECRET: eW91ci1zdHJvbmctand0LXNlY3JldC1rZXktaGVyZQ==

  # PostgreSQL connection string for the identity_schema
  # postgres://user:password@postgres-service.data.svc.cluster.local:5432/watup_db?search_path=identity_schema&sslmode=require
  DATABASE_URL: cG9zdGdyZXM6Ly91c2VyOnBhc3N3b3JkQHBvc3RncmVzLXNlcnZpY2UuZGF0YS5zdmMuY2x1c3Rlci5sb2NhbDo1NDMyL3dhdHVwX2RiP3NlYXJjaF9wYXRoPWlkZW50aXR5X3NjaGVtYQ==
